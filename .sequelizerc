// sequelize.config.js
const { resolve } = require('path');

module.exports = {
  'migrations-path': resolve(__dirname, 'dist', 'database', 'migrations'), // caminho para suas migrações
  'models-path': resolve(__dirname, 'src', 'models'), // caminho para seus modelos
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'), // caminho para seus seeders
  config: resolve(__dirname, 'src', 'config', 'database.ts'), // caminho para seu arquivo de configuração
};
